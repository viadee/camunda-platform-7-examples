camunda.bpm:
  client:
    base-url: http://localhost:8080/engine-rest
  admin-user:
    id: demo
    password: demo
  database:
    type: h2
  default-serialization-format: application/json
  filter:
    create: All tasks
  generic-properties:
    properties:
      initializeTelemetry: true
      telemetryReporterActivate: true
de.viadee.bpm.camunda:
  rest-api-base-path: http://localhost:8080/engine-rest/engine/default/
